schema:

  verticies:
  - users
  - cities
  - projects

  arcs:

  - source: users
    destination: cities
    link: "users.city_id = cities.id"

aliases:

in:
- users
- projects

want: ""
err: "no path between [users] and [projects]"
